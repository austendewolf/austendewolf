node_modules/
.next/
.env
.env.local
.DS_Store
*.log
.railway/
.vercel/

# Build-time-generated catalog seed payloads (Dockerfile builder writes
# these from the TypeScript source so the runner image can seed without
# shipping the .ts files).
scripts/seed.program.json
scripts/seed.schedule.json
